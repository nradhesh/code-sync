import { beforeAll, afterAll } from '@jest/globals';

beforeAll(async () => {
    // Add any global setup here (e.g., database connections)
});

afterAll(async () => {
    // Add any cleanup here (e.g., closing database connections)
}); 